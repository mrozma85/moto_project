@using Moto_Web.Models;
@model IEnumerable<ApplicationRole>


<div class="container p-4 border">
    <div class="row pb-2">
        <div class="col">
            <h1 class="text-primary"> </h1>
        </div>
        <div class="col text-end pt-1">
            <a asp-controller="RolesList" asp-action="CreateRolesList" class="btn btn-outline-primary"><i class="bi bi-plus-square-fill"></i>Dodaj nową</a>
        </div>
    </div>

    <table class="table">
        <thead>
            <tr>
                <th>
                    Nazwa:
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Name</td>

                    <td>
                        <a asp-controller="RolesList" asp-action="UpdateRolesList" asp-route-Id="@item.Id" class="btn btn-success"><i class="bi bi-pencil-square"></i></a>
                        <a asp-controller="RolesList" asp-action="DeleteRolesList" asp-route-Id="@item.Id" class="btn btn-danger"><i class="bi bi-trash-fill"></i></a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

<div class="form-group">
    <div class="col-md-10">
        <a class="btn btn-primary" href="https://localhost:7001/api/RolesListAPI/ExportExcel" download> Export do pliku Excel</a>
    </div>
    <br />
    <div class="col-md-10">
        <a class="btn btn-primary" href="https://localhost:7001/api/RolesListAPI/ExportPDF" download> Export do pliku PDF</a>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
